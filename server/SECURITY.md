# Руководство по безопасности бэкенда

## ✅ Реализованные меры безопасности

### 1. Защита заголовков (Helmet)
- Настроен Helmet для защиты от XSS, clickjacking и других атак
- Content Security Policy настроен для ограничения источников контента

### 2. Rate Limiting
- **API**: 100 запросов за 15 минут с одного IP
- **Авторизация**: 5 попыток входа за 15 минут
- **Админ-панель**: 50 запросов за 15 минут

### 3. Валидация входных данных
- Используется express-validator для всех входных данных
- Валидация регистрации, входа, товаров, параметров запросов
- Защита от SQL injection через Sequelize ORM

### 4. Защита от NoSQL Injection
- express-mongo-sanitize для очистки входных данных
- Защита от HTTP Parameter Pollution (HPP)

### 5. CORS с ограничениями
- Разрешены только указанные домены
- Настройка через переменную окружения ALLOWED_ORIGINS

### 6. JWT безопасность
- Обязательная проверка JWT_SECRET при запуске
- Улучшенная обработка ошибок токенов
- Нет дефолтных значений для секретов

### 7. Ограничение размера запросов
- Максимальный размер тела запроса: 10MB
- Защита от DoS атак через большие запросы

### 8. Удаление чувствительных данных
- Пароли удалены из config.json
- Все секреты только в переменных окружения
- Удалены console.log с чувствительными данными

## 📋 Необходимые переменные окружения

Создайте файл `.env` в папке `server/` со следующим содержимым:

```env
# Server Configuration
NODE_ENV=development
PORT=3001
HOST=0.0.0.0

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/universam205inc
# Или отдельные переменные:
DB_NAME=universam205inc
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=5432

# JWT Secret (ОБЯЗАТЕЛЬНО! Используйте длинную случайную строку)
# Генерируйте через: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Telegram Bot Configuration (опционально)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id
```

## 🔐 Генерация JWT_SECRET

Для генерации безопасного JWT_SECRET используйте:

```bash
# Linux/Mac
openssl rand -base64 32

# Windows PowerShell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
```

## ⚠️ Дополнительные рекомендации

### 1. HTTPS в production
- Обязательно используйте HTTPS в production
- Настройте редирект с HTTP на HTTPS
- Используйте сертификаты от Let's Encrypt

### 2. База данных
- Используйте сильные пароли для БД
- Ограничьте доступ к БД только с сервера приложения
- Регулярно делайте бэкапы
- Используйте SSL соединение с БД в production

### 3. Мониторинг
- Настройте логирование всех попыток авторизации
- Мониторьте rate limiting блокировки
- Отслеживайте подозрительную активность

### 4. Обновления
- Регулярно обновляйте зависимости: `npm audit` и `npm update`
- Проверяйте уязвимости: `npm audit fix`

### 5. Файл .env
- НИКОГДА не коммитьте .env в git
- Добавьте .env в .gitignore
- Используйте разные .env для development и production

### 6. Аутентификация
- Рассмотрите возможность добавления refresh tokens
- Реализуйте механизм сброса пароля
- Добавьте двухфакторную аутентификацию для админов

### 7. Логирование
- Используйте структурированное логирование (winston, pino)
- Не логируйте пароли и токены
- Храните логи безопасно

## 🚨 Что делать при обнаружении уязвимости

1. Немедленно исправьте уязвимость
2. Проверьте логи на наличие попыток эксплуатации
3. При необходимости отзовите все токены
4. Уведомите пользователей о необходимости смены паролей
5. Обновите зависимости

## 📚 Полезные команды

```bash
# Проверка уязвимостей
npm audit

# Исправление уязвимостей
npm audit fix

# Обновление зависимостей
npm update

# Проверка безопасности зависимостей
npm audit --audit-level=moderate
```


