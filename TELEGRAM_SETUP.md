# Настройка Telegram уведомлений

## Шаг 1: Создание Telegram бота

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например, "Универсам 205 Уведомления")
   - Введите username бота (должен заканчиваться на `bot`, например: `universam205_orders_bot`)
4. Получите токен бота (будет выглядеть примерно так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Получение Chat ID

### ⭐ Способ 1: Через специального бота (САМЫЙ ПРОСТОЙ)

#### Для личных сообщений:
1. Найдите бота **@userinfobot** или **@getidsbot** в Telegram
2. Начните с ним диалог, отправив любое сообщение (например, `/start`)
3. Бот сразу отправит вам ваш Chat ID (например: `123456789`)

#### Для группы/канала:
1. Добавьте бота **@userinfobot** или **@getidsbot** в вашу группу
2. Отправьте в группе команду `/start@userinfobot` (или просто упомяните бота)
3. Бот покажет Chat ID группы (обычно отрицательное число, например: `-1234567890`)
4. Скопируйте это число

### Способ 2: Через API Telegram

#### Для личных сообщений:
1. Напишите вашему боту любое сообщение (например, `/start`)
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
   Замените `<ВАШ_ТОКЕН>` на токен, полученный от @BotFather
3. Найдите в ответе значение `"chat":{"id":123456789}` - это и есть ваш Chat ID
4. Если видите пустой массив `{"ok":true,"result":[]}`, отправьте еще одно сообщение боту и обновите страницу

#### Для группы/канала:
1. Создайте группу в Telegram
2. Добавьте вашего бота в группу
3. Дайте боту права администратора (необязательно, но рекомендуется)
4. Отправьте любое сообщение в группу
5. Откройте в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
6. Найдите значение `"chat":{"id":-1234567890}` - это Chat ID группы (обычно отрицательное число)

### ⚠️ Важно:
- **Личный чат**: Chat ID - положительное число (например: `123456789`)
- **Группа/канал**: Chat ID - отрицательное число (например: `-1001234567890`)
- Для отправки в несколько чатов нужно использовать группу или настроить несколько Chat ID (см. ниже)

## Шаг 3: Настройка переменных окружения

В папке `server` создайте файл `.env` (если его нет) и добавьте:

### Для одного получателя:
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

### Для нескольких получателей:
Укажите Chat ID через запятую (без пробелов или с пробелами - неважно):

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=5925365670,702122191,477097668
```

**Пример:** Если вы хотите отправлять уведомления нескольким администраторам, соберите все их Chat ID из ответа `getUpdates` (значения `"chat":{"id":...}`) и перечислите через запятую.

## Шаг 4: Установка зависимостей

В папке `server` выполните:

```bash
npm install
```

Это установит пакет `axios`, необходимый для отправки уведомлений.

## Шаг 5: Перезапуск сервера

Перезапустите сервер:

```bash
npm run dev
```

## Формат уведомлений

При оформлении заказа в Telegram будет приходить сообщение следующего вида:

```
🛒 НОВЫЙ ЗАКАЗ №42

👤 Клиент: ivan_petrov
📍 Комната доставки: 305

📦 Товары:
• Молоко 3.2% - 2 л × 89 ₽
• Хлеб белый - 1 шт × 45 ₽
• Сыр Российский - 300 г × 420 ₽

💰 Итого: 643 ₽

🕐 Время заказа: 02.11.2025, 14:30:25
```

## Отключение уведомлений

Если нужно временно отключить уведомления, просто удалите или закомментируйте переменные `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` в файле `.env`

